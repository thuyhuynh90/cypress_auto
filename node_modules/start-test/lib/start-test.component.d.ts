import { EventEmitter, OnInit } from '@angular/core';
import { FormBuilder, FormGroup } from '@angular/forms';
import { Subject } from 'rxjs';
export declare class StartTestComponent implements OnInit {
    private fb;
    testSheet: any;
    ansFilling: boolean;
    timeUp: Subject<boolean>;
    outputFunction: EventEmitter<any>;
    finalDataList: any;
    sectionsForm: FormGroup[];
    inputForm: FormGroup;
    constructor(fb: FormBuilder);
    ngOnInit(): void;
    initForm(qid: any, code?: any): FormGroup;
    createTest(): void;
    submt(): void;
}
